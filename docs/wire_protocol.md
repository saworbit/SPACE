# Wire Protocol Specification (Placeholder)

The detailed replication wire format will be documented here. Current framing and validation logic lives in `crates/scaling/src/replication.rs` and `docs/replication.md`.
